{
  "name": "@keyra/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest",
    "test:e2e": "playwright test",
    "test:e2e:setup": "wrangler d1 execute keyra-db --local --file=../database/migrations/0001_users.sql && wrangler d1 execute keyra-db --local --file=../database/migrations/0002_organizations.sql && wrangler d1 execute keyra-db --local --file=../database/migrations/0003_org_members.sql && wrangler d1 execute keyra-db --local --file=../database/migrations/0004_sessions.sql && wrangler d1 execute keyra-db --local --file=../database/migrations/0005_session_cleanup.sql && wrangler d1 execute keyra-db --local --file=../database/migrations/0006_audit_logs.sql",
    "typecheck": "tsc --noEmit",
    "build": "tsc --noEmit",
    "lint": "eslint src --fix"
  },
  "dependencies": {
    "@hono/zod-openapi": "^0.15.0",
    "@keyra/shared-types": "workspace:*",
    "@keyra/shared-validation": "workspace:*",
    "bcryptjs": "^3.0.3",
    "hono": "^4.6.11",
    "jose": "^5.9.6",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241127.0",
    "@playwright/test": "^1.60.0",
    "@types/bcryptjs": "^3.0.0",
    "@types/node": "^25.9.3",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8",
    "wrangler": "^3.95.0"
  }
}
